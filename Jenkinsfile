pipeline {
    agent any
    stages {
        stage ('multi test') {
            steps {
                    sh "echo 'success  fea ture  test'"
                    sh "git branch"
            }
        }
    }
}
