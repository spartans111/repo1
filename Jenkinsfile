pipeline {
    agent any
    stages {
        stage ('multi test') {
            steps {
                    sh "echo 'success feature'"
                    sh "git branch"
            }
        }
    }
}
