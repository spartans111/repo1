pipeline {
    agent any
    stages {
        stage ('multi test') {
            steps {
                    sh "echo 'success  featture  test'"
                    sh "git branch"
            }
        }
    }
}
